on: [push]

jobs:
  area_job:
    runs-on: ubuntu-latest
    name: A job to calculate area
    steps:
    - name: Hello world action step
      id: hello
      uses: NateGiraudeau/area-of-triangle-action@v1
      with:
        width: 8
        height: 8
    - name: Get the output time
      run: echo "The time was ${{ steps.hello.outputs.time }}"
